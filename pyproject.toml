[project]
name = "aws-access-renewer"
version = "1.7.0"
description = "Asynchronous utility to automatically update AWS EC2 Security Group SSH rules with the current public IP."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.3",
    "rich>=13.7.0",
]
